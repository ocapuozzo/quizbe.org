{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Scope creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('scope_index') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
